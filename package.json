{
  "name": "vocab-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "build:pages": "npm run build -- --base=/vocab-trainer/",
    "preview": "vite preview --host 127.0.0.1",
    "simulate": "node --experimental-strip-types scripts/simulate.ts",
    "test": "node --experimental-strip-types --test src/study.test.ts"
  },
  "dependencies": {
    "papaparse": "^5.7.0",
    "ts-fsrs": "^5.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@types/papaparse": "^5.5.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
